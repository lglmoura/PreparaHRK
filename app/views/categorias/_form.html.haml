= simple_form_for(@categoria) do |f|
  = f.error_notification

  .form-inputs
    = f.input :nome
    = f.association :pai, label_method: :nome, value_method: :id

  .form-actions
    = f.submit "Salvar"
